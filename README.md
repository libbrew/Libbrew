<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Libbrew | The AI Alchemist for Mac</title>
    <style>
        :root { --bg: #000000; --text: #ffffff; --accent: #9d4edd; --secondary: #1d1d1f; }
        body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; background: var(--bg); color: var(--text); text-align: center; }
        
        /* Navigation */
        nav { padding: 20px; font-weight: 600; font-size: 14px; opacity: 0.8; }
        
        /* Hero Section */
        .hero { padding: 100px 20px; }
        .hero h1 { font-size: 56px; margin: 0; letter-spacing: -2px; }
        .hero p { font-size: 24px; color: #a1a1a6; margin: 10px 0 30px; }
        
        /* The "App" Button */
        .btn { 
            background: var(--accent); color: white; padding: 15px 35px; 
            border-radius: 30px; text-decoration: none; font-weight: bold; font-size: 18px;
            transition: transform 0.2s; display: inline-block;
        }
        .btn:hover { transform: scale(1.05); }

        /* Bento Grid */
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; padding: 50px; max-width: 1000px; margin: 0 auto; }
        .card { background: var(--secondary); padding: 40px; border-radius: 20px; text-align: left; }
        .card h3 { margin: 0; font-size: 24px; }
        .card p { color: #a1a1a6; line-height: 1.5; }

        /* Important Footer */
        footer { padding: 50px; color: #6e6e73; font-size: 12px; }
    </style>
</head>
<body>

    <nav>Libbrew Beta</nav>

    <div class="hero">
        <h1>Libbrew.</h1>
        <p>Turn simple thoughts into AI masterpieces.</p>
        <a href="YOUR_DIRECT_DOWNLOAD_LINK_HERE" class="btn">Download for Mac</a>
    </div>

    <div class="grid">
        <div class="card">
            <h3>100% Local.</h3>
            <p>Your prompts stay on your Mac. Powered by Ollama, zero data leaves your machine.</p>
        </div>
        <div class="card">
            <h3>Deep Brewing.</h3>
            <p>Our alchemist engine refines every word for Roblox, Midjourney, and beyond.</p>
        </div>
    </div>

    <footer>
        Built by aaryah. To install: Right-click the app and select 'Open' to verify the Alchemist.
    </footer>

</body>
</html>
